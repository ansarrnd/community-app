# DISABLED — merged into ci.yml.
# Active pipeline: .github/workflows/ci.yml
# See docs/CI_CD_PLAN.md

name: Web E2E Smoke (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — use ci.yml"
