# DISABLED — legacy Fastlane hybrid stub.
# Active pipeline: .github/workflows/ci.yml
# See docs/CI_CD_PLAN.md

name: Legacy CI/CD Stub (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — use ci.yml"
