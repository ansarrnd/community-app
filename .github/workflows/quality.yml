# DISABLED — not part of the active CI surface.
# Active pipeline: .github/workflows/ci.yml (GitHub-hosted only).
# See docs/CI_CD_PLAN.md

name: Quality Gate (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — use ci.yml"
